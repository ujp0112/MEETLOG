<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.CommunityCourseMapper">

    <resultMap id="communityCourseResult" type="model.CommunityCourse">
        <id property="id" column="course_id"/>
        <result property="title" column="title"/>
        <result property="previewImage" column="preview_image"/>
        <result property="authorName" column="author_name"/>
        <result property="authorImage" column="author_image"/>
        <result property="likes" column="likes"/>
        <collection property="tags" javaType="java.util.ArrayList"
                    select="selectTagsForCourse" column="course_id"/>
    </resultMap>
    
    <select id="selectTagsForCourse" parameterType="int" resultType="java.lang.String">
        SELECT t.tag_name 
        FROM course_tags ct
        JOIN tags t ON ct.tag_id = t.tag_id
        WHERE ct.course_id = #{course_id}
    </select>

    <select id="selectCommunityCourses" parameterType="map" resultMap="communityCourseResult">
        SELECT 
            c.course_id, c.title, c.preview_image,
            u.nickname AS author_name, 
            u.profile_image AS author_image,
            (SELECT COUNT(*) FROM course_likes cl WHERE cl.course_id = c.course_id) AS likes
        FROM courses c
        JOIN users u ON c.author_id = u.id 
        <where>
            AND c.type = 'COMMUNITY'
            <if test="query != null and query != ''">
                AND (c.title LIKE CONCAT('%', #{query}, '%') OR c.description LIKE CONCAT('%', #{query}, '%'))
            </if>
            <if test="area != null and area != ''">
                AND c.area = #{area}
            </if>
        </where>
        ORDER BY 
            c.created_at DESC
        LIMIT #{offset}, #{itemsPerPage}
    </select>

    <select id="countCommunityCourses" parameterType="map" resultType="int">
        SELECT COUNT(*)
        FROM courses c
        JOIN users u ON c.author_id = u.id
        <where>
            AND c.type = 'COMMUNITY'
            <if test="query != null and query != ''">
                AND (c.title LIKE CONCAT('%', #{query}, '%') OR c.description LIKE CONCAT('%', #{query}, '%'))
            </if>
            <if test="area != null and area != ''">
                AND c.area = #{area}
            </if>
        </where>
    </select>
</mapper>