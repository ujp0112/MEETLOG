<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="InquiryMapper">

    <!-- Result Map -->
    <resultMap id="InquiryResultMap" type="model.Inquiry">
        <id property="id" column="inquiry_id"/>
        <result property="userName" column="user_name"/>
        <result property="subject" column="subject"/>
        <result property="content" column="content"/>
        <result property="status" column="status"/>
        <result property="reply" column="reply"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <!-- 모든 문의 조회 (최신순) -->
    <select id="findAll" resultMap="InquiryResultMap">
        SELECT inquiry_id, user_name, subject, content, status, reply, created_at, updated_at
        FROM inquiries
        ORDER BY created_at DESC, inquiry_id DESC
    </select>

    <!-- 상태별 문의 조회 -->
    <select id="findByStatus" parameterType="string" resultMap="InquiryResultMap">
        SELECT inquiry_id, user_name, subject, content, status, reply, created_at, updated_at
        FROM inquiries
        WHERE status = #{status}
        ORDER BY created_at DESC, inquiry_id DESC
    </select>

    <!-- ID로 문의 조회 -->
    <select id="findById" parameterType="int" resultMap="InquiryResultMap">
        SELECT inquiry_id, user_name, subject, content, status, reply, created_at, updated_at
        FROM inquiries
        WHERE inquiry_id = #{id}
    </select>

    <!-- 문의 추가 -->
    <insert id="insert" parameterType="model.Inquiry" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO inquiries (user_name, subject, content, status)
        VALUES (#{userName}, #{subject}, #{content}, #{status})
    </insert>

    <!-- 문의 수정 -->
    <update id="update" parameterType="model.Inquiry">
        UPDATE inquiries SET
            user_name = #{userName},
            subject = #{subject},
            content = #{content},
            status = #{status},
            reply = #{reply}
        WHERE inquiry_id = #{id}
    </update>

    <!-- 문의 상태 변경 -->
    <update id="updateStatus" parameterType="map">
        UPDATE inquiries SET
            status = #{status}
        WHERE inquiry_id = #{id}
    </update>

    <!-- 문의 답변 등록 -->
    <update id="updateReply" parameterType="map">
        UPDATE inquiries SET
            reply = #{reply},
            status = #{status}
        WHERE inquiry_id = #{id}
    </update>

    <!-- 문의 삭제 -->
    <delete id="delete" parameterType="int">
        DELETE FROM inquiries WHERE inquiry_id = #{id}
    </delete>

    <!-- 전체 문의 개수 -->
    <select id="countTotal" resultType="int">
        SELECT COUNT(*) FROM inquiries
    </select>

    <!-- 상태별 문의 개수 -->
    <select id="countByStatus" parameterType="string" resultType="int">
        SELECT COUNT(*) FROM inquiries WHERE status = #{status}
    </select>

</mapper>