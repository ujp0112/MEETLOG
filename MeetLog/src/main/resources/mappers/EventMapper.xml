<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.EventMapper">

    <resultMap id="eventResult" type="model.Event">
        <id property="id" column="ID"/>
        <result property="title" column="TITLE"/>
        <result property="summary" column="SUMMARY"/>
        <result property="content" column="CONTENT"/>
        <result property="image" column="IMAGE"/>
        <result property="startDate" column="START_DATE"/>
        <result property="endDate" column="END_DATE"/>
    </resultMap>

    <select id="selectAllEvents" resultMap="eventResult">
        SELECT ID, TITLE, SUMMARY, CONTENT, IMAGE, START_DATE, END_DATE 
        FROM events 
        ORDER BY START_DATE DESC
    </select>
    
    <select id="selectEventById" parameterType="int" resultMap="eventResult">
        SELECT ID, TITLE, SUMMARY, CONTENT, IMAGE, START_DATE, END_DATE 
        FROM events 
        WHERE ID = #{id}
    </select>
</mapper>