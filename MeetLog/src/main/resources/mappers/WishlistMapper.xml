<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dao.WishlistDAO">

    <!-- 찜하기 추가 -->
    <insert id="addWishlist" parameterType="map">
        INSERT INTO wishlists (user_id, restaurant_id, created_at)
        VALUES (#{userId}, #{restaurantId}, NOW())
        ON DUPLICATE KEY UPDATE created_at = NOW()
    </insert>

    <!-- 찜하기 삭제 -->
    <delete id="removeWishlist" parameterType="map">
        DELETE FROM wishlists
        WHERE user_id = #{userId}
        AND restaurant_id = #{restaurantId}
    </delete>

    <!-- 찜 여부 확인 -->
    <select id="isWishlisted" parameterType="map" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM wishlists
        WHERE user_id = #{userId}
        AND restaurant_id = #{restaurantId}
    </select>

    <!-- 사용자의 찜 목록 조회 -->
    <select id="getUserWishlists" parameterType="map" resultType="model.Restaurant">
        SELECT r.*
        FROM restaurants r
        JOIN wishlists w ON r.id = w.restaurant_id
        WHERE w.user_id = #{userId}
        ORDER BY w.created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 레스토랑의 찜 개수 조회 -->
    <select id="getWishlistCount" parameterType="int" resultType="int">
        SELECT COUNT(*)
        FROM wishlists
        WHERE restaurant_id = #{restaurantId}
    </select>

    <!-- 사용자의 총 찜 개수 조회 -->
    <select id="getUserWishlistCount" parameterType="int" resultType="int">
        SELECT COUNT(*)
        FROM wishlists
        WHERE user_id = #{userId}
    </select>

</mapper>
