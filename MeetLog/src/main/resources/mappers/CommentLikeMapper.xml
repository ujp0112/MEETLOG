<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.CommentLikeDAO">

    <select id="checkLike" parameterType="map" resultType="int">
        SELECT id FROM comment_likes
        WHERE comment_id = #{commentId} AND user_id = #{userId}
    </select>

    <insert id="addLike" parameterType="map">
        INSERT INTO comment_likes (comment_id, user_id, created_at)
        VALUES (#{commentId}, #{userId}, NOW())
    </insert>

    <delete id="removeLike" parameterType="map">
        DELETE FROM comment_likes
        WHERE comment_id = #{commentId} AND user_id = #{userId}
    </delete>

    <select id="getUserLikeStatus" parameterType="map" resultType="boolean">
        SELECT COUNT(*) > 0 FROM comment_likes
        WHERE comment_id = #{commentId} AND user_id = #{userId}
    </select>

</mapper>