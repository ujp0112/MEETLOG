<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="FeedbackMapper">

    <resultMap id="FeedbackResultMap" type="model.Feedback">
        <id property="id" column="feedback_id"/>
        <result property="userId" column="user_id"/>
        <result property="restaurantId" column="restaurant_id"/>
        <result property="rating" column="rating"/>
        <result property="content" column="content"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <!-- 모든 피드백 조회 -->
    <select id="findAll" resultMap="FeedbackResultMap">
        SELECT feedback_id, user_id, restaurant_id, rating, content, created_at
        FROM feedbacks
        ORDER BY created_at DESC
    </select>

    <!-- ID로 피드백 조회 -->
    <select id="findById" resultMap="FeedbackResultMap" parameterType="int">
        SELECT feedback_id, user_id, restaurant_id, rating, content, created_at
        FROM feedbacks
        WHERE feedback_id = #{id}
    </select>

    <!-- 평점별 피드백 조회 -->
    <select id="findByRating" resultMap="FeedbackResultMap" parameterType="int">
        SELECT feedback_id, user_id, restaurant_id, rating, content, created_at
        FROM feedbacks
        WHERE rating = #{rating}
        ORDER BY created_at DESC
    </select>

    <!-- 피드백 생성 -->
    <insert id="insert" parameterType="model.Feedback" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO feedbacks (user_id, restaurant_id, rating, content)
        VALUES (#{userId}, #{restaurantId}, #{rating}, #{content})
    </insert>

    <!-- 피드백 삭제 -->
    <delete id="delete" parameterType="int">
        DELETE FROM feedbacks
        WHERE feedback_id = #{id}
    </delete>

    <!-- 전체 피드백 개수 -->
    <select id="countTotal" resultType="int">
        SELECT COUNT(*) FROM feedbacks
    </select>

    <!-- 평균 평점 조회 -->
    <select id="getAverageRating" resultType="double">
        SELECT COALESCE(AVG(rating), 0.0) FROM feedbacks
    </select>

</mapper>