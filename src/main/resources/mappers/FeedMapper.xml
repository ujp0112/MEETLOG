<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dao.FeedDAO">

    <!-- 피드 아이템 생성 -->
    <insert id="createFeedItem" parameterType="model.FeedItem" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO feed_items (user_id, user_nickname, user_profile_image, action_type, content, 
                               target_type, target_id, target_name, target_image, created_at, is_active)
        VALUES (#{userId}, #{userNickname}, #{userProfileImage}, #{actionType}, #{content},
                #{targetType}, #{targetId}, #{targetName}, #{targetImage}, NOW(), true)
    </insert>

    <!-- 사용자의 피드 조회 (팔로우한 사용자들의 활동) -->
    <select id="getUserFeed" parameterType="map" resultType="model.FeedItem">
        SELECT fi.*
        FROM feed_items fi
        JOIN follows f ON fi.user_id = f.following_id
        WHERE f.follower_id = #{userId}
        AND f.is_active = true
        AND fi.is_active = true
        ORDER BY fi.created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 특정 사용자의 활동 피드 조회 -->
    <select id="getUserActivityFeed" parameterType="map" resultType="model.FeedItem">
        SELECT * FROM feed_items
        WHERE user_id = #{userId}
        AND is_active = true
        ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 전체 공개 피드 조회 (인기 활동) -->
    <select id="getPublicFeed" parameterType="map" resultType="model.FeedItem">
        SELECT fi.*
        FROM feed_items fi
        JOIN users u ON fi.user_id = u.id
        WHERE fi.is_active = true
        AND u.is_active = true
        ORDER BY fi.created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 피드 아이템 삭제 -->
    <update id="deleteFeedItem" parameterType="int">
        UPDATE feed_items 
        SET is_active = false, updated_at = NOW()
        WHERE id = #{feedItemId}
    </update>

    <!-- 사용자별 피드 아이템 수 조회 -->
    <select id="getUserFeedCount" parameterType="int" resultType="int">
        SELECT COUNT(*) 
        FROM feed_items 
        WHERE user_id = #{userId}
        AND is_active = true
    </select>

    <!-- 팔로우한 사용자들의 최근 활동 조회 -->
    <select id="getFollowingActivity" parameterType="map" resultType="model.FeedItem">
        SELECT fi.*
        FROM feed_items fi
        JOIN follows f ON fi.user_id = f.following_id
        WHERE f.follower_id = #{userId}
        AND f.is_active = true
        AND fi.is_active = true
        ORDER BY fi.created_at DESC
        LIMIT #{limit}
    </select>

</mapper>
