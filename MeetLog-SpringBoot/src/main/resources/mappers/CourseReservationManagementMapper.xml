<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.CourseReservationManagementMapper">
    <resultMap id="adminReservationResult" type="model.AdminCourseReservation">
        <id property="id" column="reservation_id"/>
        <result property="courseTitle" column="course_title"/>
        <result property="participantName" column="participant_name"/>
        <result property="phone" column="phone"/>
        <result property="email" column="email"/>
        <result property="reservationDate" column="reservation_date"/>
        <result property="reservationTime" column="reservation_time"/>
        <result property="participantCount" column="participant_count"/>
        <result property="totalPrice" column="total_price"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="selectAllReservationsForAdmin" resultMap="adminReservationResult">
        SELECT 
            cr.reservation_id,
            c.title AS course_title,
            cr.participant_name,
            cr.phone,
            cr.email,
            cr.reservation_date,
            cr.reservation_time,
            cr.participant_count,
            cr.total_price,
            cr.status,
            cr.created_at
        FROM 
            course_reservations cr
        JOIN 
            courses c ON cr.course_id = c.course_id
        ORDER BY 
            cr.created_at DESC
    </select>
    
    <update id="updateReservationStatus" parameterType="map">
        UPDATE course_reservations
        SET status = #{status}
        WHERE reservation_id = #{id}
    </update>
</mapper>