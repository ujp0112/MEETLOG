<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.NotificationDAO">
    <resultMap id="notificationResultMap" type="Notification" autoMapping="true">
        <id property="id" column="id" />
        <result property="userId" column="user_id" />
        <result property="isRead" column="is_read" />
        <result property="actionUrl" column="action_url" />
        <result property="createdAt" column="created_at" />
        <result property="readAt" column="read_at" />
    </resultMap>

    <select id="findByUserId" parameterType="int" resultMap="notificationResultMap">
        SELECT * FROM notifications 
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>
    
    <update id="markAsRead" parameterType="map">
        UPDATE notifications 
        SET is_read = true, read_at = NOW()
        WHERE id = #{id} AND user_id = #{userId}
    </update>
    
    <update id="markAllAsRead" parameterType="int">
        UPDATE notifications 
        SET is_read = true, read_at = NOW()
        WHERE user_id = #{userId} AND is_read = false
    </update>
    
    <delete id="delete" parameterType="map">
        DELETE FROM notifications 
        WHERE id = #{id} AND user_id = #{userId}
    </delete>
    
    <insert id="insert" parameterType="Notification" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO notifications (user_id, title, content, type, action_url, is_read)
        VALUES (#{userId}, #{title}, #{content}, #{type}, #{actionUrl}, #{isRead})
    </insert>
    
    <select id="getUnreadCount" parameterType="int" resultType="long">
        SELECT COUNT(*) FROM notifications 
        WHERE user_id = #{userId} AND is_read = false
    </select>
</mapper>